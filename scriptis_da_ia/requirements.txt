fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.9.0
asyncpg==0.31.0

# Motor de simulacao (optcg_engine/) e scripts de auditoria/analise --
# faltavam aqui (achado 31/08/2026, instalacao em maquina nova): api.py
# so importa esses modulos dentro de handlers especificos (import lazy),
# entao `pip install -r requirements.txt` sozinho nunca acusava a falta
# ate alguem rodar de fato smoke_fast.py, /replay/demo ou qualquer coisa
# que puxe optcg_engine/decision_engine.py.
pandas==3.0.5
numpy==2.5.2
requests==2.34.2
joblib==1.6.0
# scikit-learn FALTAVA (achado 14/09/2026, ao planejar treinar em outra
# maquina): treinar_q.py e value_net.py dependem dele, e o import e lazy --
# mesma armadilha do achado de 31/08 comentado acima, que so aparece quando
# alguem de fato treina ou carrega um modelo.
#
# E FIXAR a versao importa mais aqui que nos outros: `joblib` serializa
# OBJETOS do sklearn, nao um formato neutro. Modelo salvo numa versao pode
# avisar ou falhar ao abrir em outra -- e `q_net.joblib` viaja pelo git entre
# maquinas, entao versao diferente quebra o modelo do outro lado, com um erro
# dificil de ligar a causa.
scikit-learn==1.9.0
beautifulsoup4==4.15.0